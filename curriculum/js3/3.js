/*
 * repeatingElements - takes in array of numbers, return an array of all duplicate numbers.
 * @param {array}
 * @return {array}
 */

const solution = (arr)=>{
  return 0;
}

module.exports = {
  solution
}
